@model TrustPower_Item_Booking_Web_App.Controllers.BookingProcessController.ViewModel
<!-- model IEnumerable<TrustPower_Item_Booking_Web_App.Models.Items>-->

@{
    ViewData["Title"] = "Index";
}




<h1>Index</h1>

<div class="col-md-10">

    @{




        List<SelectListItem> listItems = new List<SelectListItem>();

        List<string> UniqueItems = new List<string>();

        foreach (var item in Model.Items)
        {
            if (UniqueItems.Contains(item.ItemName.ToString()))
            {

            }
            else
            {
                UniqueItems.Add(item.ItemName.ToString());
            }



        }
        foreach (string i in UniqueItems)
        {
            listItems.Add(new SelectListItem
            {
                Text = i.ToString(),
                Value = i.ToString(),

            });
        }

    }

    @{
        IEnumerable<Bookings> books = Model.Bookings;
    }

    @{
        Bookings Booking = new Bookings();
    }


    <form asp-controller="BookingProcess" asp-action="Record">

        <select name="ItemName" asp-items="listItems" class="form-control"> </select>


        <div class="form-group">
           
            <input type="date" name="PickUpDate" id="PickUpId">
            
        </div>
        <div class="form-group">

            <input type="date" name="ReturnDate" id="ReturnDateId">
           
        </div>

        <div class="form-group">
            <input type="submit" value="Record" class="btn btn-primary form-control" />
        </div>

    </form>





</div>

